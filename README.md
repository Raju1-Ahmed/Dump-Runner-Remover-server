# Dump Runner Remover Server

Backend API for the DUMP RUNNERZ rubbish removal website.

## Run locally

```bash
npm install
npm run dev
```

Create a local `.env` file from `.env.example` before starting the server.

## API

- `GET /api/health`
- `POST /api/auth/register`
- `POST /api/auth/login`
